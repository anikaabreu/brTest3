<ul class="container-bod-list-group">
	  {{#sailing_options}}
    <li>
 
        <span>
            <input type="radio" name="article-{{{id}}}{{{cruise_line_id}}}" value=" {{{sailing_price}}}" />
      
      {{sailing_date}}
            
        </span>

        <span class="container-bod-cruise-price-style" id="container-bod-cruise-price-1">
  {{sailing_price}}
        </span>
    </li>

  		{{/sailing_options}}

</ul>
